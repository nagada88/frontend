import React from 'react';
import UploadForm from './components/UploadForm';

const App: React.FC = () => {
    return (
        <div>
            <UploadForm />
        </div>
    );
};

export default App;
